language: cpp
notifications:
  email:
    - cwelsh2@uw.edu
branches:
  only:
    - master
    - develop
    - travis-tests
services: docker

jobs:
  include:
    - stage: New build script
      language: cpp
      name: linux
      os: linux

# osx is only placeholder right now
#    - os: osx
#      name: osx
#      language: shell
#      before_install:
#        - mkdir -p ~/miniconda3
#        - wget https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh -O ~/miniconda3/miniconda.sh
#        - bash ~/miniconda3/miniconda.sh -b -u -p ~/miniconda3
#        - rm -rf ~/miniconda3/miniconda.sh
#        - ~/miniconda3/bin/conda init zsh
#      script:
#        - python roadrunner_deps_build.py $HOME

# windows is only placeholer right now
    - os: windows
#      name: windows
#      language: shell
#      python: 3.7
#      script:
#        - python roadrunner_deps_build.py $HOME
